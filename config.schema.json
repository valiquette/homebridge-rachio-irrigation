{
  "pluginAlias": "rachio",
  "headerDisplay": "Homebridge plugin for Rachio Controllers",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Accessory Name:",
        "description": "Name for the accessory",
        "type": "string",
        "required": true,
        "default": "Rachio"
      },
      "api_key": {
        "title": "Rachio API Key",
        "description": "Rachio API key, visit https://rachio.readme.io/docs/authentication for help.",
        "type": "string",
        "required": true,
        "placeholder": "API KEY"
      },
      "default_runtime": {
        "title": "Default Runtime in Minutes",
        "description": "Default number of minutes to run zone if not provided",
        "type": "integer",
        "minimum": 1,
        "maximum": 60,
        "required": true,
        "default": 3
      },
      "use_irrigation_display": {
        "title": "Display as Irrigation System in HomeKit",
        "description": "Display accessory in HomeKit as a irrigation system with sprinker zones. If unchecked; zones will display as single/separate tiles.",
        "type": "boolean",
        "required": true,
        "default": true
      },
      "show_standby": {
        "title": "Expose switch for controller standby",
        "description": "Exposes standby mode switch, when turned on all schedules will be disabled",
        "type": "boolean",
        "required": true,
        "default": true
      },
      "show_runall": {
        "title": "All zones",
        "description": "Adds a switch to run all zones for the default time",
        "type": "boolean",
        "required": true,
        "default": true
      },
      "show_schedules": {
        "title": "Expose switches for schedules",
        "description": "Exposes schedules as defined in the Rachio app as a switch for quick run",
        "type": "boolean",
        "required": true,
        "default": true
      },
      "external_IP_address": {
        "title": "External IP Address",
        "description": "External IP address for this server, used to receive webhook requests.<br/>To find this quick try https://www.myexternalip.com .",
        "type": "string",
        "format": "ipv4",
        "required": false,
        "placeholder": "xxx.xxx.xxx.xxx"
      },
      "external_webhook_port": {
        "title": "External Webhook Port",
        "description": "External port <br/>Source for receiving webhook requests.",
        "type": "number",
        "required": false,
        "default": 12453
      },
      "internal_webhook_port": {
        "title": "Internal Webhook Port",
        "description": "Internal port <br/>Destination for receiving webhook requests.",
        "type": "number",
        "required": false,
        "default": 27546
      },
      "use_basic_auth": {
        "title": "HTTP basic authentication",
        "description": "Use HTTP basic authentication.",
        "type": "boolean",
        "required": false,
        "default": false
      },
      "user": {
        "title": "Basic Authentication User",
        "type": "string",
        "required": false,
        "placeholder": "username",
        "maxLength": 32
      },
      "password": {
        "title": "Basic Authentication Password",
        "type": "string",
        "required": false,
        "placeholder": "password"
      },
      "delete_webhooks": {
        "title": "Delete all current webhook IDs",
        "description": "Use this if there is a need to delete current webhook IDs that may be causing problems. Such as errors for 'Webhook received from an unknown external id`",
        "type": "boolean",
        "required": false,
        "default": false
      }
    }
  },
  "layout": [
    {
      "type": "flex",
      "flex-flow": "row wrap",
      "items": [
        "name",
        "api_key",
        "default_runtime",
        "use_irrigation_display"
      ]
    },
    {
      "type": "fieldset",
      "title": "Extra Switches",
      "description": "Expand to expose additonal switches",
      "expandable": true,
      "items": [
        "show_standby",
        "show_schedules",
        "show_runall"
      ]
    },   
    {
      "type": "fieldset",
      "title": "Webhooks",
      "description": "Expand to configure webhooks",
      "expandable": true,
      "displayFlex": true,
      "items": [
        {
          "type": "flex",
          "flex-flow": "row wrap",
          "items": [
            "external_IP_address"
          ]
        },
        {
        "type": "flex",
        "flex-flow": "row wrap",
        "items": [
          "external_webhook_port",
          "internal_webhook_port"
        ]
      },
      {
        "type": "help",
        "helpvalue": "<i>Important note.<p class='help-block'> Your router will need to forward this external port to your internal webhook port.</p>"
      },
      {
          "key": "Extra stuff",
          "title": "Options",
          "description": "Extra webhook settings",
          "type": "flex",
          "expandable": true,
          "displayFlex": true,
          "flex-flow":"row wrap",
          "items": [
              {
                "type": "flex",
                "flex-flow": "row wrap",
                "items": [
                  "delete_webhooks"
                ]
              },
              {
                "type": "flex",
                "flex-flow": "row wrap",
                "items": [
                  "use_basic_auth"
                ]
              },
              {
              "type": "flex",
              "flex-flow": "row wrap",
              "items": [
                "user",
                "password"
              ]
            },
            {
              "type": "help",
              "helpvalue": "<p class='help-block'> Rachio supports HTTP Basic Authentication for webhooks. if checked any incoming message will need to match this user:password</p>"
            }
          ]
        }
      ]
    }
  ]
}